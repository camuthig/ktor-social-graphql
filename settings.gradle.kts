pluginManagement {
    repositories {
        gradlePluginPortal()
        maven {
            url = uri("https://dl.bintray.com/camuthig/maven")
        }
    }
}

rootProject.name = "ktor-social-graphql"
